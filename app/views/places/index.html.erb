<h1>Places</h1>

<% if @current_user != nil %>
  <a href="/places/new" class="btn btn-outline-dark mb-3">New Place</a>
<% end %>

<ul class="list-unstyled">
  <% for place in @places %>
    <li><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
  <% end %>
</ul>